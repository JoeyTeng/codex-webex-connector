OpenAI / Codex
- 请使用本地 codex binary，codex app-server 已安装并可运行

Local machine
- launchd 是否可用：yes
- 部署用户：hoteng
- 发布根目录：~/Library/Application Support/codex-webex-connector

Repos
- repo name: codex-webex-connector
  path: cwd

Policy
- 默认允许所有的 repo 
- 允许自动创建 git worktree
- 默认 sandbox policy：跟随本机
- 默认 network policy：跟随本机
- approval 是否允许 “approve for session”：允许。
